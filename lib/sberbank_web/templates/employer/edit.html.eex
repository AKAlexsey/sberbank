<h1>Edit Employer</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.employer_path(@conn, :update, @employer)) %>

<span><%= link "Back", to: Routes.employer_path(@conn, :index) %></span>
